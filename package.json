{
  "name": "leanxai-kpi-root", 
  "version": "1.0.0",
  "description": "Root package for monorepo build",
  
  "dependencies": {
    "axios": "^1.12.2"
  },
  

  "scripts": {
    "build:frontend": "npm install --prefix health-index-frontend --unsafe-perm && npm run build --prefix health-index-frontend"
  },
  

  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}